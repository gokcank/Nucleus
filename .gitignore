# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Working copies pulled from the main Project Nucleus repo for copywriting
# reference. Not the source of truth — resync manually when the main docs
# change. Never committed here to avoid two repos drifting silently.
reference/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
